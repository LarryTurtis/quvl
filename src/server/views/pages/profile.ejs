<% include ../head %>
<% include ../nav %>

    <div class="page-header text-center">
        <h1><span class="fa fa-anchor"></span> Profile Page</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
            <h3><span class="fa fa-user"></span> <%= (user.local && user.local.email) || (user.google && user.google.email) || (user.facebook && user.facebook.email) %></h3>
            <!-- check if the user has this token (is the user authenticated with this social account) -->

        </div>
    </div>

<% include ../foot %>
